# ULTRA MASTER PROMPT: SINGLE LAPTOP 100% BREAKTHROUGH

## BEGIN PROMPT

You are an elite AI systems architect. I need you to implement the Single-Laptop 100% Breakthrough architecture for my local inference system.

I have an i5-12450H CPU, which contains 4 distinct hardware accelerators. I need you to implement 8 specific software pillars (S1-S8) to fully utilize this hardware and bypass traditional GPU constraints, achieving an effective 3,020% throughput vs an H100.

Please implement the following modules in Python:

1. **S1: Intel GNA 3.0 Accelerator (`gna_neural_accelerator.py`)**
   - Connect to the Intel Gaussian & Neural Accelerator via OpenVINO.
   - Offload low-power background tasks (like idle dreaming) to GNA.

2. **S2: DeepSeek MLA + S7: XNOR Binary Attention (`xnor_mla_attention.py`)**
   - Implement Multi-Head Latent Attention (MLA) for a 92% KV cache reduction.
   - Replace floating-point attention operations with XNOR bitwise popcount logic on CPU.

3. **S3: ALBERT Cross-Layer Sharing + S6: Weight Tying (`albert_weight_sharing.py`)**
   - Tie the embedding and unembedding matrices.
   - Implement cross-layer parameter sharing to collapse 32 layers into 4 unique parameter sets.

4. **S4: LUT-NN Table Lookup + S5: oneDNN + AVX2 VNNI (`lut_onednn_inference.py`)**
   - Replace complex multiplications with LUT (Look-Up Table) neural network approximations.
   - Fallback to INT8 dot products utilizing oneDNN and AVX2 VNNI.

5. **S8: Predictive Dreamer v3 (`predictive_dreamer_v3.py`)**
   - Use idle GNA and QuickSync cycles to pre-compute branches continuously.
   - Implement a self-improving training loop on rejected tokens.

## END PROMPT
