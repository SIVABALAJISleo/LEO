# ── Full local/server requirements (NOT for Vercel) ──────── #
# Install with: pip install -r requirements-local.txt
# Requires Python <= 3.12 (these packages have no cp314 wheels)
-r requirements.txt

# ── Intel OpenVINO ────────────────────────────────────────── #
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python==0.3.30

--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
peft>=0.10
faiss-cpu>=1.7.4
numba>=0.59.0
pyopencl>=2024.1
openvino==2024.4
openvino-genai==2024.4

# ── Computer Vision ───────────────────────────────────────── #
opencv-python-headless>=4.9.0

# ── Media ─────────────────────────────────────────────────── #
moviepy>=1.0.3
scikit-fuzzy>=0.4.2
python-magic>=0.4.27

# ── Dev & Test ────────────────────────────────────────────── #
pytest>=9.0.3
pytest-asyncio>=0.23.0
hypothesis>=6.0.0
mutmut>=2.5.0
mypy>=1.10.0
bandit>=1.7.0
flake8>=7.0.0
